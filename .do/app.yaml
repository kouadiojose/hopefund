name: hopefund-banking

services:
  # Backend API (Docker)
  - name: api
    github:
      repo: kouadiojose/hopefund
      branch: claude/hopefund-banking-app-M021u
      deploy_on_push: true
    dockerfile_path: apps/api/Dockerfile
    source_dir: apps/api
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 8080
    envs:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "8080"
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        type: SECRET
      - key: JWT_SECRET
        scope: RUN_AND_BUILD_TIME
        type: SECRET
    routes:
      - path: /api

  # Frontend Web App (Static Site)
  - name: web
    github:
      repo: kouadiojose/hopefund
      branch: claude/hopefund-banking-app-M021u
      deploy_on_push: true
    source_dir: apps/web
    build_command: npm install && npm run build
    environment_slug: html
    routes:
      - path: /
